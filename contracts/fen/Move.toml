[package]
name = "fen"
edition = "2024.beta"

[dependencies]
world = { local = "../../world-contracts/contracts/world" }

# [addresses] removed — new-style manifest uses [environments] or auto-resolution
